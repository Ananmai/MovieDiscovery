{
  "name": "movie-discovery-app",
  "version": "1.0.0",
  "description": "Full-stack movie discovery application built with React, Node.js, and SQLite",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "test": "cd backend && npm test",
    "build": "npm install --prefix frontend --no-package-lock && npm run build --prefix frontend",
    "vercel-build": "npm install --prefix frontend --no-package-lock && npm run build --prefix frontend"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
